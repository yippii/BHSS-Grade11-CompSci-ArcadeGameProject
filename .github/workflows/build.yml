name: Build executable with Nuitka
on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Github Checkout
        uses: actions/checkout@v6

      - name: Setting Up Environment
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - run: pip install -r requirements.txt

      - name: Compile with Nuitka
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: fate_crimson_isle.py
          mode: app
          include-package: rich
          include-module: pygame.mixer
          enable-plugins: tk-inter
          include-data-files: '*.mp3=F4T3.mp3'
          output-dir: .
          output-file: fate-crimson-isle
          enable-console: true
          macos-app-name: "Fate: Crimson Isle"
          macos-app-icon: "FATE.icns"
          windows-icon-from-ico: "FATE.ico"
          linux-icon: "FATE.png"
          
      - name: Uploading artifact
        uses: actions/upload-artifact@v7
        with:
          name: Game Executable - ${{matrix.os}}
          path: | 
            ${{ matrix.os == 'windows-latest' && '*.exe' || matrix.os == 'ubuntu-latest' && 'fate-crimson-isle' || matrix.os == 'macos-latest' && '*.app' }}
            F4T3.mp3
